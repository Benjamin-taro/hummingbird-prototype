# Hummingbird 発音ビジュアライザー — 仕様書

## 1. プロジェクト概要

### 1.1 目的
英文を入力すると、各音素に対応する **ハミングバード式の口の形（Mouth Position: MP）** と **舌の位置（Tongue Placement: TP）** を視覚的に表示するウェブアプリケーションを構築する。

### 1.2 想定ユーザー
- ハミングバード発音矯正メソッドで学習中（または学習予定）の日本人英語学習者
- 自主練習時に「この単語／フレーズはどのMP・TPで発音するか」を即座に確認したい人

### 1.3 技術スタック（推奨）
- **フロントエンド**: React + TypeScript + Tailwind CSS（または Next.js）
- **音素変換**: CMU Pronouncing Dictionary（cmudict）ベースの ARPAbet → IPA 変換
- **IPA → MP/TP マッピング**: アプリ内 JSON データとして保持
- **口・舌の図解**: SVG アニメーション（口の形8種 + 舌の位置8種を図示）
- **ホスティング**: Vercel / Netlify / GitHub Pages 等の静的ホスティング

---

## 2. ハミングバード記号体系（コアデータ）

### 2.1 口の形（Mouth Position） — 8種

| ID | MP名 | 視覚的特徴 | 対応IPA音素（代表） |
|----|-------|-----------|-------------------|
| MP1 | **Open** | 口を大きく縦に開く | /ɑː/, /æ/, /aɪ/, /aʊ/ |
| MP2 | **Triangle** | 口角を横に引き、やや縦に開く（▽型） | /iː/, /ɪ/, /eɪ/, /ɛ/, /j/ |
| MP3 | **Base** | ニュートラルな中間位置（やや開き） | /ə/, /ʌ/, /oʊ/（開き側）, /ɔː/ |
| MP4 | **Pucker** | 唇をすぼめて前に突き出す | /ɹ/, /ɝː/, /ɚ/ |
| MP5 | **O-shape** | 唇を丸く突き出す（O型） | /uː/, /ʊ/, /w/, /oʊ/（閉じ側） |
| MP6 | **Lips-in** | 上下の唇を合わせる（内側に巻き込む） | /p/, /b/, /m/ |
| MP7 | **Bottom-lip-in** | 下唇を上前歯に当てる | /f/, /v/ |
| MP8 | **Closed** | 唇を閉じ気味にし歯を近づける | /s/, /z/, /ʃ/, /ʒ/, /tʃ/, /dʒ/ |

### 2.2 舌の位置（Tongue Placement） — 8種

| ID | TP名 | 舌の接触点 | 対応IPA音素（代表） |
|----|-------|----------|-------------------|
| TP1 | **Zero** | 舌はどこにも触れない（退避） | 母音全般, /w/, /h/, /p/, /b/, /m/, /f/, /v/ |
| TP2 | **C** | 舌先を下前歯の裏に軽く置く | /s/, /z/ |
| TP3 | **E** | 舌の両側が上の奥歯に触れる（後部歯茎付近） | /ʃ/, /ʒ/, /tʃ/, /dʒ/ |
| TP4 | **Th** | 舌先を上下の歯の間に出す | /θ/, /ð/ |
| TP5 | **L** | 舌先を上の歯茎（前歯の裏）に付ける | /l/ |
| TP6 | **TDN** | 舌先を歯茎に強く弾く/押し当てる | /t/, /d/, /n/ |
| TP7 | **K** | 舌の奥を軟口蓋（上あご奥）に付ける | /k/, /ɡ/, /ŋ/ |
| TP8 | **R** | 舌を巻く or 奥に引く（どこにも触れない） | /ɹ/, /ɝː/, /ɚ/ |

### 2.3 IPA → MP/TP マッピングテーブル（JSON形式）

アプリ内部では以下のような形式でマッピングを保持する:

```jsonc
{
  "phonemeMap": {
    // 母音
    "ɑː": { "mp": "Open",      "tp": "Zero" },
    "æ":  { "mp": "Open",      "tp": "Zero" },
    "aɪ": { "mp": "Open→Triangle", "tp": "Zero" },  // 二重母音（遷移）
    "aʊ": { "mp": "Open→O-shape",  "tp": "Zero" },  // 二重母音（遷移）
    "ɛ":  { "mp": "Triangle",  "tp": "Zero" },
    "eɪ": { "mp": "Triangle",  "tp": "Zero" },       // 二重母音
    "iː": { "mp": "Triangle",  "tp": "Zero" },
    "ɪ":  { "mp": "Triangle",  "tp": "Zero" },
    "ə":  { "mp": "Base",      "tp": "Zero" },
    "ʌ":  { "mp": "Base",      "tp": "Zero" },
    "ɔː": { "mp": "Base",      "tp": "Zero" },
    "oʊ": { "mp": "Base→O-shape", "tp": "Zero" },    // 二重母音（遷移）
    "uː": { "mp": "O-shape",   "tp": "Zero" },
    "ʊ":  { "mp": "O-shape",   "tp": "Zero" },
    "ɝː": { "mp": "Pucker",    "tp": "R" },
    "ɚ":  { "mp": "Pucker",    "tp": "R" },

    // 子音
    "p":  { "mp": "Lips-in",       "tp": "Zero" },
    "b":  { "mp": "Lips-in",       "tp": "Zero" },
    "m":  { "mp": "Lips-in",       "tp": "Zero" },
    "f":  { "mp": "Bottom-lip-in", "tp": "Zero" },
    "v":  { "mp": "Bottom-lip-in", "tp": "Zero" },
    "θ":  { "mp": "Base",          "tp": "Th" },
    "ð":  { "mp": "Base",          "tp": "Th" },
    "s":  { "mp": "Closed",        "tp": "C" },
    "z":  { "mp": "Closed",        "tp": "C" },
    "ʃ":  { "mp": "Closed",        "tp": "E" },      // or Pucker — 要調整
    "ʒ":  { "mp": "Closed",        "tp": "E" },
    "tʃ": { "mp": "Closed",        "tp": "E" },
    "dʒ": { "mp": "Closed",        "tp": "E" },
    "t":  { "mp": "Base",          "tp": "TDN" },
    "d":  { "mp": "Base",          "tp": "TDN" },
    "n":  { "mp": "Base",          "tp": "TDN" },
    "l":  { "mp": "Base",          "tp": "L" },
    "k":  { "mp": "Base",          "tp": "K" },
    "ɡ":  { "mp": "Base",          "tp": "K" },
    "ŋ":  { "mp": "Base",          "tp": "K" },
    "ɹ":  { "mp": "Pucker",        "tp": "R" },       // confirmed
    "w":  { "mp": "O-shape",       "tp": "Zero" },
    "j":  { "mp": "Triangle",      "tp": "Zero" },
    "h":  { "mp": "Base",          "tp": "Zero" }
  }
}
```

> **注意**: 二重母音（diphthong）は口の形が遷移するため、`"Open→Triangle"` のように2つのMPを `→` で連結して表現する。UIではアニメーション遷移として表示する。

---

## 3. 機能要件

### 3.1 英文入力 & 音素変換

| 機能 | 詳細 |
|------|------|
| テキスト入力 | テキストエリアに英文（1文〜数文）を入力 |
| 音素変換エンジン | CMU Pronouncing Dictionary（cmudict）を使用し、単語ごとに ARPAbet を取得。ARPAbet → IPA への変換テーブルを内部に持つ |
| 未知語の処理 | cmudictに未登録の単語は、g2p（grapheme-to-phoneme）ライブラリでフォールバック。それでも変換不可の場合は「?」マークで表示 |
| ストレス情報 | ARPAbetのストレスマーク（0/1/2）を保持し、UIで強勢音節をハイライト |

### 3.2 メインビジュアライザー画面

```
┌─────────────────────────────────────────────────────┐
│  🐦 Hummingbird Pronunciation Visualizer            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [英文入力テキストエリア]                              │
│  [変換ボタン]                                        │
│                                                     │
├─────────────────────────────────────────────────────┤
│                                                     │
│  英文表示エリア（単語ごとにクリッカブル）                 │
│  "The   quick   brown   fox   jumps"                │
│   ðə   kwɪk   bɹaʊn   fɑːks  dʒʌmps              │
│                                                     │
├──────────────────────┬──────────────────────────────┤
│                      │                              │
│   [口の形 SVG図]      │   [舌の位置 SVG図]            │
│                      │                              │
│   現在のMP: Open      │   現在のTP: Zero              │
│                      │                              │
├──────────────────────┴──────────────────────────────┤
│                                                     │
│  音素シーケンスバー（タイムライン風）                     │
│  [ð][ə] [k][w][ɪ][k] [b][ɹ][aʊ][n] ...           │
│   ▲ 現在位置                                        │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 3.3 インタラクション仕様

| 操作 | 動作 |
|------|------|
| 単語クリック | その単語の先頭音素にジャンプし、口・舌の図を更新 |
| 音素タイルクリック | 該当音素のMP/TPを表示。口・舌のSVGがアニメーション遷移 |
| ◀ / ▶ ボタン | 前後の音素に1つずつ移動 |
| ▶ 再生ボタン | 音素を一定間隔（0.5秒〜調整可能）で自動送り。口の形がアニメーションで遷移 |
| 速度スライダー | 自動再生の速度を調整（0.2秒〜2.0秒 / 音素） |

### 3.4 SVGビジュアル仕様

#### 口の形（正面図）
- 顔の正面イラスト（鼻下〜あご）をSVGで描画
- 8つのMP状態を、唇のパスデータ切り替え + CSS transition で表現
- 各MPの特徴を誇張して描き分ける（例: Puckerは唇を前に突き出すアニメーション）

#### 舌の位置（断面図）
- 口腔の矢状断面（横から見た図）をSVGで描画
- 上あご、歯、歯茎、硬口蓋、軟口蓋を固定描画
- 舌のパスを8つのTP状態で切り替え（接触点をハイライト）
- 接触点に赤い丸ドットを表示し、「ここに舌を当てる」を直感的に示す

### 3.5 情報パネル

各音素選択時に表示する情報:

- **IPA記号**: /ɹ/
- **MP名**: Pucker（口をすぼめて前に突き出す）
- **TP名**: R（舌を巻く/奥に引く）
- **ワンポイントアドバイス**: 「唇を突き出しながら舌先をどこにも触れずに奥に引きます」
- **発音記号の信頼度**: confirmed / 推定（deep research の結果に基づく）

---

## 4. データパイプライン

```
英文テキスト
    │
    ▼
┌──────────────┐
│ Tokenizer    │  単語分割 + 句読点処理
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ CMU Dict     │  単語 → ARPAbet 列
│ Lookup       │  (例: "fox" → F AO1 K S)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ ARPAbet→IPA  │  ARPAbet → IPA 変換
│ Converter    │  (例: F→/f/, AO1→/ɔː/, K→/k/, S→/s/)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ IPA→MP/TP    │  IPA音素 → { mp, tp } マッピング
│ Mapper       │  (例: /f/→{Bottom-lip-in, Zero})
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ UI Renderer  │  SVG描画 + アニメーション + テキスト表示
└──────────────┘
```

---

## 5. 非機能要件

| 項目 | 要件 |
|------|------|
| レスポンシブ | モバイル（375px）〜 デスクトップ（1440px）対応 |
| パフォーマンス | 初回ロード < 3秒、音素変換 < 100ms |
| オフライン対応 | cmudict データをバンドルに含め、API不要で動作 |
| アクセシビリティ | キーボードナビゲーション対応、ARIA ラベル |
| 多言語UI | 日本語 / 英語 切り替え（ラベルや説明文） |
| ブラウザ対応 | Chrome / Safari / Firefox / Edge 最新版 |

---

## 6. ファイル構成（参考）

```
hummingbird-visualizer/
├── public/
│   └── cmudict.json            # CMU辞書データ（ビルド時に生成）
├── src/
│   ├── app/
│   │   └── page.tsx            # メインページ
│   ├── components/
│   │   ├── TextInput.tsx       # 英文入力
│   │   ├── PhonemeDisplay.tsx  # 英文+IPA表示エリア
│   │   ├── MouthVisual.tsx     # 口の形SVG（8状態）
│   │   ├── TongueVisual.tsx    # 舌の位置SVG（8状態）
│   │   ├── PhonemeTimeline.tsx # 音素シーケンスバー
│   │   ├── InfoPanel.tsx       # 音素情報パネル
│   │   └── PlaybackControls.tsx # 再生コントロール
│   ├── data/
│   │   ├── phonemeMap.ts       # IPA → { mp, tp } マッピング
│   │   ├── arpabetToIpa.ts     # ARPAbet → IPA 変換テーブル
│   │   └── mpTpDescriptions.ts # MP/TPの日本語説明・アドバイス
│   ├── lib/
│   │   ├── tokenizer.ts        # テキスト分割
│   │   ├── cmudictLookup.ts    # cmudict検索
│   │   └── g2pFallback.ts      # 未知語フォールバック
│   └── svg/
│       ├── mouth/              # 8種の口SVGデータ
│       └── tongue/             # 8種の舌断面SVGデータ
├── scripts/
│   └── buildCmudict.ts         # cmudictをJSONに変換するビルドスクリプト
├── package.json
├── tsconfig.json
└── tailwind.config.ts
```

---

## 7. フェーズ分け（開発ロードマップ）

### Phase 1 — MVP（最小実行可能版）
- テキスト入力 → cmudict → IPA変換 → MP/TP表示（テキストのみ）
- 音素クリックで情報パネル更新
- 口の形のシンプルなSVG図（8状態の静的切り替え）

### Phase 2 — ビジュアル強化
- 舌の断面SVG追加
- SVGアニメーション（状態間のスムーズ遷移）
- 二重母音の遷移アニメーション
- 自動再生機能 + 速度調整

### Phase 3 — UX改善
- レスポンシブデザイン完成
- 日本語/英語 UI切り替え
- ワンポイントアドバイス充実
- よく使うフレーズのプリセット（"Nice to meet you" 等）

### Phase 4 — 発展機能（Optional）
- Web Speech API による音声入力対応
- 音声合成（TTS）と口の形の同期再生
- ユーザーの発音録音 → 比較表示
- PWA対応（オフラインインストール）

---

## 8. 既知の制約・注意事項

1. **マッピングの確度**: deep research の結果、公式に全64セル（8MP×8TP）がIPA対応で公開されているわけではない。特に TP の C, E の対応は推定に基づく。アプリ内で `confirmed` / `推定` をラベル表示し、ユーザーに透明性を担保する。

2. **cmudict の限界**: 固有名詞、新語、スラングは未登録の場合がある。g2p フォールバックも完璧ではないため、未変換語は明示する。

3. **音素の文脈依存**: 実際の発話では前後の音素により口の形が影響を受ける（コアーティキュレーション）。本アプリは各音素を独立して表示するため、実際の連続発話とは差異がある。

4. **著作権**: ハミングバード教材そのもの（動画、レッスン音声等）は著作権保護対象。本アプリは特許情報（公開文献）と音声学的知見に基づく独自実装であり、教材の複製ではない。

---

## 9. Claude Code への指示サマリー

Claude Code に渡す際のプロンプト例:

```
この仕様書（hummingbird-app-spec.md）に基づいて、
ハミングバード発音ビジュアライザーのウェブアプリを作ってください。

技術スタック: React + TypeScript + Tailwind CSS（Vite）
優先度: Phase 1（MVP）をまず完成させてください。

要件:
1. 英文を入力すると単語ごとにIPA音素に変換される
2. 各音素をクリックすると口の形（MP）と舌の位置（TP）が
   SVG図として表示される
3. 音素シーケンスを順番に送れるナビゲーション
4. phonemeMap.ts のマッピングデータは仕様書 §2.3 を参照
5. UIは日本語メインで

まずは仕様書を読み込んだ上で、
ファイル構成と実装計画を提示してから着手してください。
```

---

*最終更新: 2026-03-01*
*ベースデータ: deep-research-report.md（ハミングバード発音矯正メソッドの一次情報ベース分析）*
